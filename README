A connection string will need to be provided at the bottom of web.config, and a database will need to be created and setup using the .sql files (run Schema.sql first). Instructions will be provided later as to how to set up the database.

LICENSE: See LICENSE file